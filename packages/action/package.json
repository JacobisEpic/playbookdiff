{
  "name": "@playbookdiff/action",
  "version": "0.1.0",
  "private": true,
  "description": "GitHub Action wrapper around the PlaybookDiff Git regression engine.",
  "license": "MIT",
  "type": "module",
  "sideEffects": false,
  "scripts": {
    "build": "tsdown",
    "clean": "node -e \"import('node:fs').then(({ rmSync }) => rmSync('dist', { recursive: true, force: true }))\"",
    "test": "vitest run",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "@actions/core": "1.11.1",
    "@playbookdiff/core": "workspace:*",
    "playbookdiff": "workspace:*"
  },
  "devDependencies": {
    "yaml": "2.9.0"
  },
  "engines": {
    "node": ">=24.11.0 <25"
  }
}
